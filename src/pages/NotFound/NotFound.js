import { NotFoundContainer, PageNotFound } from "../NotFound/NotFound.styled";

export default function NotFound() {
  return (
    <NotFoundContainer>
      <PageNotFound>
        We're sorry, the page what you looking for is not exist
      </PageNotFound>
    </NotFoundContainer>
  );
}
